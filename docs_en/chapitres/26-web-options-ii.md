# Web / Options (II)

- Select the standard set (by default UTF-8)

- Check the \"Use persistent connections\" option

- Enter the number of requests per connection (by default 100)

- Enter the delay before disconnection (by default 15 seconds)
