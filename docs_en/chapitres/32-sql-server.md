# SQL server

While data access for a 4D client uses a proprietary protocol by
default, SQL access (native or via ODBC) is also well supported. In
addition, there are open source PDO drivers available (PHP Data
Objects). SQL access at the database level can be controlled by a
password system, SQL schemas or using SQL views.
